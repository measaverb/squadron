# Squadron - visualization app (app.py) dependencies.
# NOTE: the graded algorithm modules (scheduler.py = Module 1, allocator.py = Module 2)
#       use the Python standard library ONLY and need NONE of these.
#       These are required solely for the Streamlit visualisation layer.
streamlit>=1.30
pandas>=1.5
altair>=5.0
